mutation SetMolliePaymentMethod($cartId: String!) {
  setPaymentMethodOnCart(input: { cart_id: $cartId, payment_method: { code: "mollie" } }) {
    cart {
      selected_payment_method {
        ...SelectedPaymentMethod
      }
    }
  }
}
