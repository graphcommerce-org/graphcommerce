fragment MollieAvailablePaymentMethod on AvailablePaymentMethod
@inject(into: ["AvailablePaymentMethod"]) {
  mollie_available_issuers {
    code
    image
    name
    svg
  }
  mollie_meta {
    image
  }
}
