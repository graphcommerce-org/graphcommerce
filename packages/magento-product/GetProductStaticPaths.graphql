query GetProductStaticPaths($currentPage: Int!) {
  products(filter: {}, pageSize: 1000, currentPage: $currentPage) {
    page_info {
      current_page
      total_pages
    }
    total_count
    items {
      url_key
    }
  }
}
