fragment ProductListItem on ProductInterface @injectable {
  uid
  ...ProductLink
  sku
  name
  small_image {
    url
    label
  }
  price_range {
    minimum_price {
      ...ProductListPrice
    }
  }
}
