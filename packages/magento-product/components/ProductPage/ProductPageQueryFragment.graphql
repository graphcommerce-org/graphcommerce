fragment ProductPageQueryFragment on Query @injectable {
  products(filter: { url_key: { eq: $urlKey } }) {
    ...ProductSpecs
    items {
      __typename
      uid
      ...ProductWeight
      ...ProductPageItem
    }
  }
}
