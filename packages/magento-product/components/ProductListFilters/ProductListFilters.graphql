fragment ProductListFilters on Products @injectable {
  aggregations {
    __typename
    label
    attribute_code
    count
    options {
      __typename
      label
      value
      count
    }
  }
}
