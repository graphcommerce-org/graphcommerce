# import ProductListFilters from './ProductListFilters.graphql'
fragment ProductFiltersQueryFragment on Query {
  filters: products(filter: { category_uid: { eq: $categoryUid } }) {
    ...ProductListFilters
  }
}
