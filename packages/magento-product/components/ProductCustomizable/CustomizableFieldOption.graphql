# import CustomizableOption from './CustomizableOption.graphql'

fragment CustomizableFieldOption on CustomizableFieldOption {
  ...CustomizableOption
  product_sku
  fieldValue: value {
    max_characters
    price
    price_type
    sku
    uid
  }
}
