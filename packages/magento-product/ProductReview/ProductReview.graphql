fragment ProductReview on ProductInterface {
  reviews(pageSize: 10, currentPage: $reviewPage) {
    items {
      average_rating
      created_at
      nickname
      ratings_breakdown {
        name
        value
      }
      summary
      text
    }
  }
  review_count
  rating_summary
}
