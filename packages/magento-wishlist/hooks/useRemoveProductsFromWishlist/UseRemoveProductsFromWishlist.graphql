mutation UseRemoveProductsFromWishlist($wishlistItemIds: [ID!]!) {
  removeProductsFromWishlist(wishlistId: 0, wishlistItemsIds: $wishlistItemIds) {
    user_errors {
      code
      message
    }
    wishlist {
      ...Wishlist
    }
  }
}
