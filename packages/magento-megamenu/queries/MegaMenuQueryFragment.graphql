fragment MegaMenuQueryFragment on Query {
  menu: categories(filters: { parent_category_uid: { eq: $rootCategory } }) {
    items {
      ...MegaMenuItem
      children {
        uid
        ...MegaMenuItem

        children {
          uid
          ...MegaMenuItem
        }
      }
    }
  }
}
