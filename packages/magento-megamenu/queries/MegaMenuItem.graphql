fragment MegaMenuItem on CategoryTree {
  uid
  include_in_menu
  name
  url_path
  children_count
  children {
    uid
    include_in_menu
    name
    url_path
    children_count
    children {
      uid
      include_in_menu
      name
      url_path
      children_count
      children {
        uid
        include_in_menu
        name
        url_path
        children_count
        children {
          uid
          include_in_menu
          name
          url_path
          children_count
        }
      }
    }
  }
}
