fragment DownloadableCartItem on DownloadableCartItem @injectable @inject(into: ["CartItem"]) {
  customizable_options {
    ...SelectedCustomizableOption
  }

  links {
    uid
    price
    sample_url
    title
  }
  samples {
    sample_url
    sort_order
    title
  }
}
