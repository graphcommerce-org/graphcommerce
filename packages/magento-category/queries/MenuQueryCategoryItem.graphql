fragment MenuQueryCategoryItem on CategoryTree {
  uid
  ...NavigationItem
  children {
    uid
    ...NavigationItem
    children {
      uid
      ...NavigationItem
      children {
        uid
        ...NavigationItem
      }
    }
  }
}
