fragment CategoryQueryFragment on Query {
  categories(filters: { url_path: { eq: $url } }) {
    items {
      uid
      default_sort_by
      ...CategoryDefault
      ...CategoryBreadcrumb
      ...CategoryDescription
      ...CategoryChildren
      ...CategoryMeta
      ...CategoryHeroNav
    }
  }
}
