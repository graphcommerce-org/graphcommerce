fragment MenuQueryFragment on Query @injectable {
  menu: categories {
    items {
      uid
      ...NavigationItem
      children {
        uid
        ...NavigationItem
        children {
          uid
          ...NavigationItem
          children {
            uid
            ...NavigationItem
          }
        }
      }
    }
  }
}
