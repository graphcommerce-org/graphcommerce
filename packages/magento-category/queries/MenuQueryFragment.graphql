fragment MenuQueryFragment on Query {
  menu: categories(filters: { parent_category_uid: { eq: $rootCategory } }) {
    items {
      ...NavigationItem
      children {
        uid
        ...NavigationItem
        children {
          uid
          ...NavigationItem
        }
      }
    }
  }
}
