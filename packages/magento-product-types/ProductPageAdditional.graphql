query ProductPageAdditional($urlKey: String) {
  productAdditionals: products(filter: { url_key: { eq: $urlKey } }) {
    ...ProductSpecs
    items {
      upsell_products {
        ...ProductListItemRenderer
      }
      related_products {
        ...ProductListItemRenderer
      }
    }
  }
}
