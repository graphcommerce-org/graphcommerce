fragment ViewCartFragment on Cart @inject(into: ["CartItemCountChanged"]) {
  id
  prices {
    grand_total {
      currency
      value
    }
  }
  items {
    ...CartItemToGoogleDatalayerItem
  }
}
