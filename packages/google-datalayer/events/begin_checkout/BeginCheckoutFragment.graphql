fragment BeginCheckoutFragment on Cart @inject(into: ["CartStartCheckout"]) {
  prices {
    grand_total {
      currency
      value
    }
  }
  applied_coupons {
    code
  }
  items {
    ...CartItemToGoogleDatalayerItem
  }
}
