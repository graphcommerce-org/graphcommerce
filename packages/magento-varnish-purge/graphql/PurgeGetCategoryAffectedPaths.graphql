query PurgeGetCategoryAffectedPaths($categoryIds: [String]!) {
  categories(filters: { ids: { in: $categoryIds } }) {
    items {
      id
      url_path
      products(pageSize: 100000) {
        items {
          id
          url_key
        }
      }
    }
  }
}
