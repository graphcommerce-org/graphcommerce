fragment ProductPageVirtualQueryFragment on Query {
  typeProducts: products(filter: { url_key: { eq: $urlKey } }) {
    ...ProductSpecs
    items {
      uid
      __typename
      ... on VirtualProduct {
        ...ProductCustomizable
      }
    }
  }
}
