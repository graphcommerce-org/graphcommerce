type HistoryStatePage {
  href: String!
  as: String!
  x: Int!
  y: Int!
  title: String!
}

enum Direction {
  BACK
  FORWARD
}

enum Phase {
  LOADING
  LOCATION_CHANGED
  REGISTERED
  FINISHED
}

type HistoryState {
  idx: Int!
  pages: [HistoryStatePage!]!
  direction: Direction!
  phase: Phase!
}

extend type Query {
  historyState: HistoryState!
}
