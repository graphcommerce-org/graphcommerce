fragment SimpleCartItem on SimpleCartItem @inject(into: ["CartItem"]) {
  uid
  customizable_options {
    customizable_option_uid
    label
    values {
      customizable_option_value_uid
      value
    }
  }
}
