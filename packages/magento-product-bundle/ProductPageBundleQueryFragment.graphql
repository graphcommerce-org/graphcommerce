fragment ProductPageBundleQueryFragment on Query {
  typeProducts: products(filter: { url_key: { eq: $urlKey } }) {
    ...ProductSpecs
    items {
      ... on BundleProduct {
        __typename
        ...ProductWeight
        ...ProductCustomizable
        ...BundleItems
      }
    }
  }
}
