fragment ProductPageBundleQueryFragment on Query @injectable {
  typeProducts: products(filter: { url_key: { eq: $urlKey } }) {
    items {
      __typename
      uid
      ...BundleProductOptions
    }
  }
}
