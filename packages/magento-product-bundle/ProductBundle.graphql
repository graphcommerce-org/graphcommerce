query ProductBundle($urlKey: String) {
  bundleProducts: products(filter: { url_key: { eq: $urlKey } }) {
    items {
      ... on BundleProduct {
        ...ProductWeight
        ...ProductCustomizable
        ...BundleItems
      }
    }
  }
}
