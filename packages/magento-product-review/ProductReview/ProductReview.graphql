fragment ProductReview on ProductInterface @inject(into: ["ProductPageItem"]) {
  reviews(pageSize: 10, currentPage: $reviewPage) {
    items {
      average_rating
      created_at
      nickname
      ratings_breakdown {
        name
        value
      }
      summary
      text
    }
  }
}
