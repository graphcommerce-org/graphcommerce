fragment PickupLocationitems on Cart @inject(into: ["AvailableShippingMethods"]) {
  items {
    product {
      sku
    }
  }
  shipping_addresses {
    pickup_location_code
    postcode
    country {
      code
    }
  }
}
