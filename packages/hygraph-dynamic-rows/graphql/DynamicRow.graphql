fragment DynamicRow on DynamicRow @injectable {
  placement
  target {
    ... on Node {
      id
    }
  }

  rows {
    __typename
    ... on Node {
      id
    }

    ...RowColumnOne
    ...RowQuote
    ...RowLinks
  }
}
