fragment BraintreePaymentMethodContext on Cart @injectable @inject(into: ["PaymentMethodContext"]) {
  shipping_addresses {
    country {
      code
    }
  }
  prices {
    grand_total {
      ...Money
    }
  }
}
