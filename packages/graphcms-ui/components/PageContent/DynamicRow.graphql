query DynamicRow($rowIds: [ID!]!) {
  dynamicRows(where: { id_in: $rowIds }) {
    placement
    target {
      ... on Node {
        id
      }
    }
    row {
      __typename
      ... on Node {
        id
      }
      ...RowColumnOne
      ...RowColumnTwo
      ...RowColumnThree
      ...RowQuote
      ...RowLinks
    }
  }
}
