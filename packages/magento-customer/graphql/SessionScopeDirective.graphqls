"""
By providing these values the Apollo Client cache will automatically scope the cache to these specific values.

If a SessionScope is provided
"""
input SessionScopeInput {
  loggedIn: Boolean
  # Future extensions, not yet implemented.
  # hygraphPreviewMode: String
  # xMagentoCacheId: String
  # previewVersion: String
  # contentCurrency: String
}

"""
Magento expects certain values to be set as headers in the GraphQL request.
"""
directive @sessionScope(sessionScope: SessionScopeInput) on FIELD
