query ShipmentDetailPage($orderNumber: String!) {
  customer {
    orders(filter: { number: { eq: $orderNumber } }) {
      items {
        ...OrderDetails
        shipments {
          ...Shipment
        }
      }
    }
  }
}
