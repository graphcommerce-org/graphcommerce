mutation UpdateCustomerName($prefix: String, $firstname: String!, $lastname: String!) {
  updateCustomerV2(input: { prefix: $prefix, firstname: $firstname, lastname: $lastname }) {
    customer {
      prefix
      firstname
      lastname
    }
  }
}
