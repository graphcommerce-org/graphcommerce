fragment AccountOrders on Customer @injectable {
  orders(filter: {}, pageSize: $pageSize, currentPage: $currentPage) {
    items {
      ...OrderCard
    }
    page_info {
      current_page
      page_size
      total_pages
    }
    total_count
  }
}
