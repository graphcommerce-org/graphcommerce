fragment ConfigurableOptions on ConfigurableProduct @injectable {
  ...ConfigurableOptionsBase
  variants {
    attributes {
      value_index
      uid
      code
    }
    product {
      uid
      sku
      name
      small_image {
        ...ProductImage
      }
    }
  }
}
