fragment DefaultConfigurableOptionsSelection on Products {
  items {
    uid
    __typename
    url_key
    ...ProductPageGallery
    ...ConfigurableOptions

    ... on SimpleProduct {
      ...ProductPageItem
    }
  }
}
