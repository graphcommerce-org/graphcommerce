fragment DefaultConfigurableOptionsSelection on Products @injectable {
  items {
    uid
    __typename
    url_key
    ...ProductPageGallery
    ...ConfigurableOptions

    ... on SimpleProduct {
      ...ProductPageItem
    }
  }
}
