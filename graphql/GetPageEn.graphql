query GetPageEN($url: String!) {
  page(where: { urlEN: $url }) {
    metaTitle(locale: EN)
    metaDescription(locale: EN)
    metaRobots
    url(locale: EN)
    urlNL: url(locale: NL)
    blogPost {
      publicPublishedAt
      image {
        height
        width
        url
        handle
      }
      title(locale: EN)
      content(locale: EN)
      author {
        firstName
        lastName
        email
      }
    }
  }
}
