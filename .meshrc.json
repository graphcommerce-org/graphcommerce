{
  "sources": [
    {
      "handler": {
        "graphql": {
          "headers": {
            "Authorization": "${GRAPHCMS_BEARER}"
          },
          "endpoint": "${GRAPHCMS}"
        }
      },
      "name": "GraphCMS",
      "transforms": [
        {
          "filterSchema": ["Mutation.createContactForm"]
        }
      ]
    }
  ]
}
