---
'@graphcommerce/next-config': patch
---

Fixed issue where additional props on a plugin did not carry to the final component. Now we can extend the original component's props with additional props from plugins.
