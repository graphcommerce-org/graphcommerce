---
'@graphcommerce/algolia-products': patch
---

Solve issue where Algolia didn't properly handle visibility as the attribute wasn't filterable, it automatically detects when there is a visibility attribute and uses that for filtering.
