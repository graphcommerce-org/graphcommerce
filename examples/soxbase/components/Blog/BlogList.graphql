query BlogList($currentUrl: [String!]!, $first: Int = 100) {
  blogPosts: pages(
    where: { url_starts_with: "blog", url_not_in: $currentUrl }
    orderBy: publishedAt_DESC
    first: $first
  ) {
    ...BlogItem
  }
}
