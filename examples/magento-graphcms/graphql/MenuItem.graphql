fragment MenuItem on CategoryTree {
  uid
  name
  url_path
  include_in_menu
  children_count

  children {
    uid
    include_in_menu
    name
    url_path
    children_count
    children {
      uid
      name
      include_in_menu
      url_path
    }
  }
}
