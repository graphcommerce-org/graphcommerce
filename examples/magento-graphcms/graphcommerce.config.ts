import type { GraphCommerceConfig } from '@graphcommerce/next-config'

const conf: GraphCommerceConfig = {
  buildFlags: {
    advancedFilters: false,
    singleProductRoute: true,
  },
}

export default conf
