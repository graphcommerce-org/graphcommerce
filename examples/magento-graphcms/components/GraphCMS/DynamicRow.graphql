query DynamicRow($rowIds: [ID!]!) {
  dynamicRows(where: { id_in: $rowIds }) {
    placement
    target
    row {
      __typename
      ... on Node {
        id
      }
      ...RowColumnOne
      ...RowColumnTwo
      ...RowColumnThree
      ...RowQuote
      ...RowLinks
    }
  }
}
