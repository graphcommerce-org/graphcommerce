fragment FooterQueryFragment on Query @inject(into: ["LayoutFragment"]) {
  footer(where: { identity: "footer" }) {
    id
    socialLinks {
      ...PageLink
    }
    copyright
    legalLinks {
      ...PageLink
    }
  }
}
