fragment ConfigurableCartitem on ConfigurableCartItem {
  configurable_options: customizable_options {
    id
    is_required
    label
    sort_order
    values {
      label
      price {
        type
        units
        value
      }
      value
    }
  }
}
