# In this file we register the data requirements for the component.
# Autocomplete should work here, else you can look it up in GraphCMS
fragment HeroBanner on HeroBanner {
  __typename
  id
  video {
    url
    mimeType
  }
  content {
    html
  }
  title {
    html
  }
}
