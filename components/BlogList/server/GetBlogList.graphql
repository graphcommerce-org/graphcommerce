query GetBlogList($url: String!, $locale: Locale!) {
  blogPosts: pages(
    where: { url_starts_with: $url }
    locales: [$locale]
    orderBy: releaseDate_DESC
    first: 100
  ) {
    ...BlogListItem
  }
}
