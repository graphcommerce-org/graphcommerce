query GetPageLayout($url: String!, $locale: Locale!) {
  pages(where: { url: $url }, locales: [$locale]) {
    ...PageLayout
  }
  header(where: { identity: "header" }) {
    ...Header
  }
  footer: footer(where: { identity: "footer" }, locales: [$locale]) {
    ...Footer
  }
}
