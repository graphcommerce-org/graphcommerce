type HistoryStatePage {
  href: String!
  as: String!
  x: Int!
  y: Int!
  holdBackground: Boolean!
  title: String!
}

enum Direction {
  BACK
  FORWARD
}

enum Phase {
  LOADING
  LOCATION_CHANGED
  REGISTERED
}

type HistoryState {
  idx: Int!
  pages: [HistoryStatePage!]!
  direction: Direction!
  phase: Phase!
}

extend type Query {
  historyState: HistoryState!
}
