fragment SimpleCartItem on SimpleCartItem {
  id
  quantity
  prices {
    ...CartItemPrices
  }
  product {
    sku
    name
    url_key
    small_image {
      url
      label
    }
  }
  customizable_options {
    ...SelectedCustomizableOption
  }
}
