query StoreConfig {
  storeConfig {
    id

    base_currency_code
    default_display_currency_code

    title_suffix
    title_prefix
    title_separator
    default_title

    cms_home_page

    catalog_default_sort_by
    category_url_suffix
    product_url_suffix

    root_category_id

    weight_unit
  }
}
