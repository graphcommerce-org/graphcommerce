import React from 'react'

export default function CategorySort(props: GQLCategorySortFragment) {
  // console.log(props)
  const { automatic_sorting, available_sort_by, default_sort_by } = props
  return <div />
}
